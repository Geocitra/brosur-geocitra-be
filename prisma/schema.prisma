generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model ProductShowcase {
  id           String   @id @default(uuid())
  slug         String   @unique
  name         String   
  tagline      String   
  primaryColor String   
  blocks       Json     

  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  // [OPTIMASI ANALIS]
  // Menambahkan explicit B-Tree index untuk mempercepat pencarian data berdasarkan slug
  @@index([slug])
  
  @@map("product_showcases")
}